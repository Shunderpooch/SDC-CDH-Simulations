
public class CD_EmbeddedSystem extends Module implements CD{
	//add any specific embedded system fields here:
	//
	//
	
	//basic constructor using only the super inherited fields
	public CD_EmbeddedSystem(double mass, double cost, String protocol, double powerCapacity){
		super(mass, cost, protocol, powerCapacity);
	}
}
